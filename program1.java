class program1 {
    public static void main(String[] args) {
       int first = 10;
       int second = 20;
       // add two numbers
       int sum = first + second;
       System.out.println(first + " + " + second + " = "  + sum); //+ means concatenation operator
     }
   }

   //javac is the compiler which generates the .class file,taken by the jvc to create the executable